(ns four-clojure.easy.problem-126-test
        (:require [clojure.test :refer :all]))

(declare __)

(deftest problem-126
  (testing "Through the Looking Class"
    (is (let [x __]
          (and (= (class x) x) x)))
    ))

(def __

  Class

  )